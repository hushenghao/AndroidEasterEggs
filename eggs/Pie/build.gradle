plugins {
    id("easter.egg.library")
}

android {
	namespace = "com.android_p.egg"
	resourcePrefix = "p_"

	sourceSets {
		main {
			res.srcDirs = ['res']
			java.srcDirs = ['src']
			manifest.srcFile 'AndroidManifest.xml'
		}
	}
}

dependencies {
    implementation(libs.androidx.appcompat)
}
