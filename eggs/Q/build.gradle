plugins {
    id("easter.egg.library")
}

android {
	namespace = "com.android_q.egg"
	resourcePrefix = "q_"

	sourceSets {
		main {
			res.srcDirs = ['res']
			java.srcDirs = ['src']
			manifest.srcFile 'AndroidManifest.xml'
		}
	}
}

dependencies {
    implementation(libs.androidx.appcompat)
}
